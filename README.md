# Receipts App

### Build application
If you want to build the application open the project folder in terminal and run command:
`gradlew clean build`

### Run application
If you want to run the application first build it then run in the same terminal the following command:
`gradlew run`